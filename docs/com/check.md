# CHECK

The command `CHECK` is used to check the ['INPUT'](./input.md) data for inconsistencies in values of any parameters or flags.

Note that this is a necessary but not sufficient test of the validity of your input data.