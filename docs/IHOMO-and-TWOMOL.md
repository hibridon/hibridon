If IHOMO = .TRUE., then the calculation is assumed to involve a homonuclear molecule.

The flag TWOMOL should be set
  - .TRUE. if the collision involves two molecules, both with internal structure.
  - .FALSE. if the collision partner is a structureless particle.