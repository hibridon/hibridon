The flag `FLAGHF` must be set .TRUE. if the angular momentum of the molecule is half-integer.

In this case the internal values of the **integer** variables [`JTOT`](jtot), its projection, and other appropriate angular momenta are equal to the true values **minus** 1/2.