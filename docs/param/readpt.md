# Flag READPT

If `READPT` = `.TRUE.`, then the [potential](../potlist) subroutine is expecting to read data stored in a file whose name is given on the last line of the [input](../INPUT.md) data file.

If `READPT` = `.FALSE.`, then the last line of the [input](../INPUT.md) data file is ignored.
