# Flag READPT

If `READPT` = `.TRUE.`, then the [potential](../potlist.md) subroutine is expecting to read data stored in a file whose name is given on the last line of the [input](../com/input.md) data file.

If `READPT` = `.FALSE.`, then the last line of the [input](../com/input.md) data file is ignored.
